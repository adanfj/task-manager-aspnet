type Task = { id?: number, title: string, description: string, isCompleted: boolean, createdAt?: string, updatedAt?: string, dueDate?: string }

namespace App{
    type Locals = {
        token:string
    }
}